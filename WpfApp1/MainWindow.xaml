<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:gmap="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        Title="Solar Power Visualization" Height="600" Width="1100">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <Button Content="Browse CSV" Width="120" Margin="0,0,10,0" Click="BrowseCsvButton_Click"/>
            <TextBlock x:Name="CsvFileNameText" VerticalAlignment="Center" Foreground="#666"/>
      
        </StackPanel>
        

        <!-- Time Series Chart -->
        <lvc:CartesianChart Grid.Row="1" Grid.Column="0" Series="{Binding Series}" LegendLocation="Right" Grid.ColumnSpan="2" Margin="0,0,500,0">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Local Time" Labels="{Binding Labels}" LabelsRotation="45"/>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Power (MW)"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>

        <!-- Map -->
        <gmap:GMapControl x:Name="MapControl" Grid.Row="1" Grid.Column="1" />
    </Grid>
</Window>